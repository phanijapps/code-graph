# code-graph-query — required deps
# Python 3.12+. The query skill is read-only and has no parsing dependencies.

# Vector search inside SQLite (only needed if the index has embeddings).
sqlite-vec>=0.1.6,<0.2

# Optional: only needed if you want to run semantic `--search` against embeddings.
# Omit this to stay lightweight; FTS + graph + AST queries work without it.
sentence-transformers>=3.0,<4
